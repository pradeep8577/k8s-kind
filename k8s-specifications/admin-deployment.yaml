apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: admin
    spec:
      containers:
        - name: frontend
          image: pradeep018/amcibus.admin
          ports:
            - containerPort: 80

